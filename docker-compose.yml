version: '3.8'

services:
  vue-app:
    build: .
    container_name: vue-app
    ports:
      - "5174:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
